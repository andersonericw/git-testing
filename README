rebase:
create a branch off master, add a file to it and commit, do not push
create a second branch off master, make a change, commit and push
checkout your first branch and go a rebase to the second branch which incorporates these changes
